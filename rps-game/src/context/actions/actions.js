const chooseButton = "CHOOSE_BUTTON";
const displayResult = "DISPLAY_RESULT";
const clearChoices = "CLEAR_CHOICES";

const appActions = {
  chooseButton,
  displayResult,
  clearChoices,
};

export default appActions;
